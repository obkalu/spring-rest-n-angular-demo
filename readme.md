This is Customer Data Management webapp.

It is a simple demo client/server webapp demonstrating an Angular client webapp consuming/presenting data from a Spring-REST backend service with CORS enabled.